{
  "name": "empleados-api",
  "dockerComposeFile": "./docker-compose.dev.yml",
  "service": "backend",
  "shutdownAction": "none",
  "customizations": {
    "vscode": {
      "extensions": ["esbenp.prettier-vscode", "dbaeumer.vscode-eslint", "Prisma.prisma"],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  },
  "postCreateCommand": "sudo npm i npm -g && npm i && npm run db:build",
  "workspaceFolder": "/app"
}
